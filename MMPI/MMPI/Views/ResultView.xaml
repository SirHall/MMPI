<UserControl x:Class="MMPI.ResultView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:views="clr-namespace:MMPI.Views"
             d:DesignHeight="300" d:DesignWidth="300"
             mc:Ignorable="d">
  <Grid>
    <ListView Margin="{StaticResource Margin_NormalThickness}"
              BorderThickness="0"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              ItemsSource="{Binding Path=ResultList,
                                    Mode=OneWay}">
      <ListView.ItemTemplate>
        <DataTemplate>
            <views:ScaleControl />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</UserControl>
